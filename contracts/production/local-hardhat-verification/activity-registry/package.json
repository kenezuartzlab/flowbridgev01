{
  "name": "flowbridge-verify-activity-registry",
  "version": "1.0.0",
  "private": true,
  "description": "Frozen Hardhat verification project for FlowBridgeActivityRegistry on BOT Mainnet 677",
  "scripts": {
    "compile": "../node_modules/.bin/hardhat compile",
    "hashes": "node scripts/hashes.js",
    "verify": "../node_modules/.bin/hardhat run scripts/verify.js --network bot"
  }
}
